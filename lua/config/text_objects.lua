-- ============================================================================
-- Mini.AI configuration
-- ============================================================================
-- Keep defaults! Text objects use operator-pending mode, which doesn't
-- conflict with Colemak navigation (normal/visual modes).
require('mini.ai').setup()
